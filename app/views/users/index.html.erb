<p id="notice"><%= notice %></p>

<% provide(:title, "All Users") %>

<ul class="users">
  <%= render @users %>
</ul>
<%= paginate @users %>
<br>

<%= link_to 'New User', new_user_registration_path %>
